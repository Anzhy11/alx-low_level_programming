#include "main.h"
#include <string.h>
/**
 * _strlen - a function that returns the length of a string.
 * @s: an input integer pointer.
 * Return: string length
*/

int _strlen(char *s)
{
	return (strlen(s));
}
